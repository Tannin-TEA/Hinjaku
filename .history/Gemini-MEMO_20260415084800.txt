# Gemini-MEMO: 開発作業用スクラッチパッド

## 【現在進行中のタスク】
PHASE 1: 構造分解（Structural Decomposition）

## 【ユーザーからの命令】
1. 挙動を変えずにリファクタリングすること。
2. 以下の3層に分離すること：
   - 入出力層（CLI / ファイル / 外部依存）
   - コアロジック層（純粋関数）
   - データ構造層
3. main関数は「呼び出しだけ」にする。
4. 各関数の責務を1つに限定する。
5. 依存関係は「外 → 内」の一方向に限定する。

## 【Geminiの提案：モジュール構成案】
### 1. データ構造層 (Data Layer)
- `config.rs`: `Config` 構造体、`SortMode` 等の定義。
- `archive.rs`: `ImageEntry` 構造体の定義。

### 2. コアロジック層 (Core Logic Layer)
- `archive.rs`: 判定ロジック（`is_image_ext`, `natord` 等）。
- `manager.rs`: 管理ロジック（`schedule_prefetch`, `apply_sorting` 等）。
- ※OSやUIに依存しない「純粋な計算」をここに集約。

### 3. 入出力層 (I/O Layer)
- `main.rs`: アプリの起動、引数受取、他モジュールの呼び出し。
- `integrator.rs` (新規): Windows固有処理（二重起動防止、ShellExecuteW等）。
- `config.rs`: INIファイルの物理的な読み書き。
- `viewer.rs`: eguiによるUI描画とイベント受付。

## 【対話・修正履歴】
- [2024-xx-xx]: Gemini-MEMO.txt を作成。構造分解の要件を転記。
- [2024-xx-xx]: 3層分離の設計合意。以下の変更点を確定。
    - `main.rs` からアイコン生成と二重起動防止を `integrator.rs` へ移動。
    - コマンドライン引数 `-c` (または `--config`) により、読み込む INI ファイル名を切り替える機能を実装。
    - 設定項目に「KeyConfig」セクションを追加予定。これも INI ごとに独立させる。
    - `viewer.rs` から外部プロセス起動を `integrator.rs` へ移動。
    - 設定管理を TOML から `rust-ini` による INI形式へ変更（憲法第2条準拠）。
    - `chrono` ライブラリを廃止し、標準の `SystemTime` を使用（軽量化）。

## 【決定事項（実行待ち）】
1. Cargo.toml: serde, toml, chrono を削除。rust-ini を追加。
2. integrator.rs: OS依存（Win32）の全機能をここに集約。
3. config.rs: INI読み書きとデータ構造に再編。
4. main.rs: integrator と viewer を呼び出すだけの薄いエントリポイントに変更。